// Jenkinsfile (Declarative Pipeline)
/* Requires the Docker Pipeline plugin */
pipeline {
    // what is any?
    agent any

    // pipeline start
    stages {
        stage('build') {
            steps {
                sh 'echo "Hello World"'
                sh 'java -version'
            }
        }
    }
}
